# Shared
POSTGRES_DSN=postgresql://telemetry:telemetry@postgres:5432/telemetry
REDIS_URL=redis://redis:6379/0

# Auth
JWT_SECRET=dev-secret-change-me
JWT_ISSUER=device-telemetry-alerts
JWT_AUDIENCE=device-telemetry-alerts

# Queue backend: redis_streams (default), sqs, kafka_stub
QUEUE_BACKEND=redis_streams

# Optional AWS (only used if QUEUE_BACKEND=sqs or STATE_BACKEND=dynamodb)
AWS_REGION=us-west-2
SQS_QUEUE_URL=
DYNAMODB_TABLE=
