apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: telemetry
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16
          env:
            - name: POSTGRES_DB
              value: telemetry
            - name: POSTGRES_USER
              value: telemetry
            - name: POSTGRES_PASSWORD
              value: telemetry
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: telemetry
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
